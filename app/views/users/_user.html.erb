<li>
  <%= gravatar_for user, size: Settings.gravatar.size_small %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t("users.title_delete"), user, method: :delete,
    data: {confirm: t("users.confirm_delete")} %>
  <% end %>
</li>
