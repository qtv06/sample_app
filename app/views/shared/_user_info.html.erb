<%= link_to gravatar_for(current_user, size: Settings.gravatar.size_small), current_user %>
<h1><%= current_user.name %></h1>
<p><%= link_to t("users.view_profile") %></p>
<p><%= pluralize(current_user.microposts.count, t("micropost.title_micro")) %></p>
